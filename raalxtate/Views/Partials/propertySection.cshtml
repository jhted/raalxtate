@inherits Umbraco.Web.Mvc.UmbracoViewPage<PropertyList>

@model IPublishedContent


@{
    // var Agent = Model.Value<Agent>("selectAgent");
    // var blogList = Model;
}

@*

    <strong>@Model.Name</strong>




<!-- Property Section Begin -->
<section class="property-section spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="section-title">
                    <h4>PROPERTY Grid</h4>
                </div>
            </div>
        </div>
        <div class="row">
            @foreach (var property in blogList)
            {
                @Html.Partial("~/Views/Partials/propertyBox.cshtml")
            }
            <div class="col-lg-12">
                <div class="blog-pagination property-pagination ">
                    <a href="#">1</a>
                    <a href="#">2</a>
                    <a href="#">3</a>
                    <a href="#" class="icon"><span class="arrow_right"></span></a>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Property Section End -->

*@